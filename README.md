# Synthetic Lab Report Generator

**Onco-Navi** 서비스의 답변 품질 검증을 위한 가상 검사지(PDF) 및 메타데이터(JSON) 생성기입니다.

## 🎯 목적
가상의 유방암 환자 시나리오를 기반으로, 실제 병원에서 발급된 것처럼 보이는 검사지를 반복적으로 생성하여 AI 서비스가 적절하게 반응하는지 테스트합니다.

## ✨ 주요 기능
- **가상 데이터 생성**: 실제 임상 범위(Reference Range)를 기반으로 한 현실적인 혈액 검사 수치(CBC, CMP, Tumor Marker) 생성
- **시나리오 기반**: 진단(Diagnosis), 치료 중(On-Treatment), 추적 관찰(Surveillance) 등 다양한 임상 상황 시뮬레이션
- **PDF 출력**: 환자가 업로드할 법한 병원 스타일의 검사지 PDF 생성
- **JSON 메타데이터**: 검증을 위한 정답 데이터 및 기대 답변 포인트 제공

## 🚀 설치 및 실행

### 필수 요구 사항
- Node.js (v14 이상)
- npm

### 설치
```bash
npm install
```

### 실행
```bash
npx ts-node src/index.ts
```

실행이 완료되면 `output/` 디렉토리에 결과물이 생성됩니다.
- `output/json/`: 구조화된 환자 및 검사 데이터
- `output/pdf/`: 생성된 검사지 PDF

## 📂 프로젝트 구조
```
src/
├── data/           # 참조 범위(Reference Range) 등 상수 정의
├── generators/     # 데이터 생성 로직 (Lab Values, Scenarios)
├── models/         # TypeScript 인터페이스 정의
├── renderer/       # PDF 생성 로직 (PDFKit 사용)
└── index.ts        # 메인 실행 파일
```

## 📝 시나리오 목록
현재 7개의 시나리오가 정의되어 있습니다. (`src/generators/scenario-definitions.ts`)
1. **Diagnosis**: 초기 진단 (정상 / 종양표지자 상승)
2. **On-Treatment**: 항암 치료 중 (호중구 감소 / 빈혈 / 간수치 상승)
3. **Surveillance**: 추적 관찰 (정상 / 재발 의심)
